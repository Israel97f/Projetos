[package]
name = "lua_ui_bridge"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mlua = { version = "0.11", features = ["lua55", "module", "mlua_derive"]}
iced = { version = "0.14.0", features = ["canvas", "tokio"] }